name: Rust

defaults:
  run:
    shell: bash
    working-directory: scripts

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - run: pwd
      - run: source ./build-fns
      - name: Build
        run: build-image service
